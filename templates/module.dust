import protobuf = require("protobufjs");

export interface ProtoBufModel extends protobuf.ProtoBuf {
	toArrayBuffer(): ArrayBuffer;
	toBase64(): string;
	toString(): string;
}

export interface ProtoBufBuilder {
	{#definitions}
	{name}: {type};
	{/definitions}
}

{#messages}
{>interface:./}
{/messages}

{#enums}
{>enum:./}
{/enums}
