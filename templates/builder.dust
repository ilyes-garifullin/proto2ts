export interface {name}Builder extends ProtoPackageBuilder {
	new(): {name};
	decode(buffer: ArrayBuffer) : {name};
	decode64(buffer: string) : {name};
	{#definitions}
	{name}: {type};
	{/definitions}
}
